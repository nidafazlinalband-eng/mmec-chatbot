Flask>=2.0.0
requests>=2.28.0
# Optional: google-generative-ai (if using Gemini SDK) or openai (if using OpenAI endpoints)
# If you want AI responses using OpenAI, ensure OPENAI_API_KEY is set in env
openai>=0.27.0

# For loading a local .env file during development
python-dotenv>=1.0.0

# reportlab is optional (used for PDF reports). Uncomment to enable PDF generation
# reportlab>=3.6.12
